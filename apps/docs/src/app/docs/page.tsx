import { LeftSidebarLayout } from "@repo/ui"
import { SideNav } from "../components/sidenav"

export default function Page({ children }: { children: React.ReactNode }) {
  return <LeftSidebarLayout>hello</LeftSidebarLayout>
}
