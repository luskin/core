{
  "name": "core",
  "private": true,
  "scripts": {
    "-----CLEAN-----": "-----CLEAN-----",
    "clean": "turbo clean && rm -rf node_modules .turbo && pnpm i",
    "-----DEPENDENCY MANAGEMENT-----": "-----DEPENDENCY MANAGEMENT-----",
    "add:frontend": "pnpm add --filter frontend",
    "add:backend": "pnpm add --filter backend",
    "add:utils": "pnpm add --filter @core/utils",
    "remove:frontend": "pnpm remove --filter frontend",
    "-----BUILD-----": "-----BUILD-----",
    "build": "turbo build",
    "build:frontend": "turbo build --filter frontend",
    "build:backend": "turbo build --filter backend",
    "build:packages": "turbo build --filter=./packages/*",
    "codegen": "pnpm -r --filter backend run codegen",
    "-----DEVELOPMENT-----": "-----DEVELOPMENT-----",
    "docker:nuke": "docker ps -q | awk '{print $1}' | xargs -o docker stop",
    "docker:start-clean": "yarn docker:nuke && docker compose up --force-recreate -V",
    "docker": "docker compose up",
    "dev": "turbo dev",
    "dev:api": "turbo dev --filter backend -- -c apps/api/nest-cli.json",
    "dev:frontend": "turbo dev --filter frontend --filter api",
    "dev:utils": "turbo dev --filter @core/utils",
    "ui:firebase": "open http://127.0.0.1:9098/auth",
    "db:migrate": "pnpm -r --filter backend run migrate",
    "-----START-----": "-----START-----",
    "start": "turbo start",
    "start:frontend": "turbo start --filter frontend",
    "start:api": "turbo start --filter backend -- -c apps/api/nest-cli.json",
    "----FORMAT-----": "----FORMAT-----",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "-----TEST-----": "-----TEST-----",
    "test:utils": "turbo test --filter @core/utils"
  },
  "devDependencies": {
    "@configs/eslint": "workspace:*",
    "@configs/typescript": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "^1.11.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=20.9.0"
  }
}
