services:
  #
  # Web Docs
  #
  - type: web
    name: mothership-web-docs-production
    branch: production
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:docs
    startCommand: pnpm start:docs
  - type: web
    name: mothership-web-docs-staging
    branch: staging
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:docs
    startCommand: pnpm start:docs

  #
  # Web App
  #
  - type: web
    name: mothership-web-app-production
    branch: production
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:web
    startCommand: pnpm start:web
    envVars:
      - key: BASE_API_URL
        value: https://mothership-api-production.onrender.com
  - type: web
    name: mothership-web-app-staging
    branch: staging
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:web
    startCommand: pnpm start:web
    envVars:
      - key: BASE_API_URL
        value: https://mothership-api-staging.onrender.com

  #
  # api Services
  #
  - type: web
    name: mothership-api-production
    branch: production
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:api
    startCommand: pnpm start:api
    envVars:
      - key: PORT
        value: 5000
  - type: web
    name: mothership-api-staging
    branch: staging
    plan: free
    env: node
    buildCommand: pnpm i; pnpm build:api
    startCommand: pnpm start:api
    envVars:
      - key: PORT
        value: 5000
